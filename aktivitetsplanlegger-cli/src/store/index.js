import { createStore } from 'vuex'



export default createStore({
  // store the data of our app, all the components have access
  state: {
  },
  // methods which change the data that's in the state
  // mutation takes state as argument, and one additional argument (payload)

  mutations: {
  },
  actions: {
  },
  modules: {

  }
})